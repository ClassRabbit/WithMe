<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorButtonDonotChoice</item>
        <item name="colorPrimaryDark">@color/colorButtonChoice</item>
        <item name="colorAccent">@color/colorButtonChoice</item>
    </style>

</resources>
